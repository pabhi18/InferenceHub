mode: standalone

replicas: 1

rootUser: minioadmin
rootPassword: minioadmin123

persistence:
  enabled: true
  size: 10Gi
  storageClass: standard   

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "1"
    memory: "1Gi"

service:
  type: ClusterIP
  port: 9000

consoleService:
  type: ClusterIP
  port: 9001