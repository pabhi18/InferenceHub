apiVersion: v1
kind: Service
metadata:
  name: {{ include "vllm-platform.fullname" . }}
  labels:
    {{- include "vllm-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vllm-platform.selectorLabels" . | nindent 4 }}